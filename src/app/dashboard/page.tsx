import {FC} from 'react' 
import Button from '../components/ui/Button'

interface pageProps {}

const page: FC<pageProps> = ({})=>{
  return <Button size='default'>hello</Button>
}

export default page